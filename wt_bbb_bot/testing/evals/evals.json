{
  "skill_name": "bug-bash-bot",
  "evals": [
    {
      "id": 1,
      "type": "scenario",
      "scenario_file": "scenarios/checkpoint_assertion.md",
      "expected_behavior": "Classifies as Crash/Assertion type in txn subsystem. Extracts the assertion text (ts_order->ts_seconds != 0) and stack trace from the provided log. Forms a hypothesis about timestamp ordering invariant violation during checkpoint. Recommends paths/investigate.md for deep-dive. Notes blast radius (8 variants, 25% failure rate, blocking trunk) as High urgency."
    },
    {
      "id": 2,
      "type": "scenario",
      "scenario_file": "scenarios/disagg_hang.md",
      "expected_behavior": "Classifies as Hang/Timeout type on disagg storage path. Notes the hang point is after 'Flushing dirty pages to page service' — maps to page service / flow control. Searches Glean or references disagg-page-inspection skill. Recommends paths/investigate.md with specific questions about WT stall or page service flow control. Notes 70% failure rate as Critical urgency."
    },
    {
      "id": 3,
      "type": "scenario",
      "scenario_file": "scenarios/priority_ranking.md",
      "expected_behavior": "Routes to paths/priority.md or priority agent. Ranks BF-SCENARIO-C (disagg hang, 70% rate, unowned, blocking milestone) as #1, BF-SCENARIO-A (checkpoint crash, 25% rate, trunk blocking) as #2, BF-SCENARIO-B (flaky, 3% rate, assigned, not blocking) as #3. Produces ranked table with blast radius, recurrence, age, owner columns. Gives one concrete next action per top-3."
    },
    {
      "id": 4,
      "type": "fixture",
      "fixture_dir": "WT-16620",
      "prompt": "WT-16620",
      "expected_behavior": "Fetches WT-16620 via jira_get_issue. Extracts failure from ticket description: test_cursor_random.test_cursor_random_deleted_all raises WiredTigerError: Invalid argument on macos-14-arm64. Notes Evergreen logs are unavailable (permission error) and falls back to the description. Classifies as Assertion/API error. Forms a hypothesis about cursor insert returning Invalid argument — likely a timestamp or configuration issue. Notes Build Baron has no data. Produces structured output."
    }
  ]
}
