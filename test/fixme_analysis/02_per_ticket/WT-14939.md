# FIXME Analysis: WT-14939

## Jira Status
- **Status**: Backlog
- **Summary**: test_tiered18 not working under disagg
- **Priority**: Major - P3
- **Introduced**: 2025-07-08 by a4f10c8e (Will Korteland, WT-14697 Merge disagg testing code)

## FIXME Occurrences
| File:Line | FIXME text |
|-----------|------------|
| test/suite/test_tiered18.py:96 | this should be reinstated at some point. (entire `test_tiered_shared` test body is commented out) |

## What this FIXME asks for
The `test_tiered_shared` test method in `test_tiered18.py` was gutted as part of WT-14967 when tiered shared storage was adapted for the disaggregated storage path. The test body that validates creating a tiered shared table, checking its metadata, altering it, and verifying log state is entirely commented out. The FIXME asks for the test to be reinstated in a form that is compatible with disaggregated storage, so that tiered-shared-table creation and metadata correctness do not go untested.

## Importance: MEDIUM
**Reasoning**: Tiered shared table creation is a user-visible feature. With the entire test commented out, any regression in shared tiered table metadata (colgroup configuration, log enable/disable, schema entries) would go undetected. The test itself is not complex to restore but requires understanding the disaggregated storage path's metadata layout.

## Test Coverage: UNCOVERED
**Relevant tests**: test/suite/test_tiered18.py (the test exists but is completely commented out)
**Gap**: No active test validates shared tiered table creation, metadata contents, alter behavior, or log-enable state under the disaggregated storage hook.
