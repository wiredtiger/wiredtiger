# FIXME Analysis: WT-16532

## Jira Status
- **Status**: Open
- **Summary**: Investigate failure from bug010
- **Priority**: Major - P3
- **Introduced**: 2026-01-29 by efcad181 (peter.macko@mongodb.com)

## FIXME Occurrences
| File:Line | FIXME text |
|-----------|------------|
| test/suite/test_bug010.py:47 | FIXME-WT-16532 The setup of this test with the disagg hook causes a hard-to-reproduce failure. |

## What this FIXME asks for
The `test_checkpoint_dirty` test in `test_bug010.py` was disabled under the disagg hook because it started failing after WT-16458. The FIXME asks for investigation into whether this failure is a real bug exposed by WT-16458 or a pre-existing problem, and whether the test is even worth running under disagg given it uses backup cursors (which are not supported in layered trees). Until the root cause is understood and backup cursor support is clarified, the test is skipped via `@wttest.skip_for_hook("disagg", ...)`.

## Importance: MEDIUM
**Reasoning**: This is an unanswered test failure for a checkpoint consistency test under the disagg hook. The test verifies that data is consistent across multiple checkpoints with concurrent insertions — a fundamental correctness property. If the failure reflects a real bug, it could indicate checkpoint consistency issues specific to disagg table creation. The ticket has linked BFGs (via WT-16602 DUP). Scheduled for sprint 2026-06-19.

## Test Coverage: PARTIAL
**Relevant tests**: test/suite/test_bug010.py itself
**Gap**: The test is entirely skipped under the disagg hook. The root cause of the failure under disagg is not understood. No alternative test covers the same checkpoint consistency scenario for layered tables.
