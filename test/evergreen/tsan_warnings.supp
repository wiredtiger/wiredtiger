# Suppress the listed TSan warnings. Load this file with 
# export TSAN_OPTIONS="suppressions=path/to/wiredtiger/test/evergreen/tsan_warnings.supp"
# before running tests with TSan

# FIXME-WT-XXXX TSan is raising warnings about mutexes that are non-intuitive. These will be investigated separately.
mutex:src

# FIXME-WT-XXXX The mutex code is a particularly hot path. Review these changes in a separate ticket.
race:src/support/mtx_rw.c