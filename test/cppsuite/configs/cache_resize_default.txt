# Configuration for cache_resize:
# - Defines a thread that uses the custom operation to resize the cache during runtime.
# - Defines several insert threads are used to perform large transactions.
# - Defines custom tracking to save the operations and their timestamps.
cache_max_wait_ms=500,
cache_size_mb=500,
duration_seconds=30,
workload_generator=
(
    custom_config=
    (
        op_rate=10s,
        thread_count=1
    ),
    insert_config=
    (
        key_size=10000,
        op_rate=5s,
        ops_per_transaction=(min=1,max=1),
        thread_count=10,
        value_size=2000000
    ),
    populate_config=
    (
        # Only one empty collection is required.
        collection_count=1
    ),
),
workload_tracking=
(
    # Timestamp
    tracking_key_format=Q,
    # Operation type, cache size
    tracking_value_format=iQ
)