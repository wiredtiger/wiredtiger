# Configuration for cache_resize:
# - Defines a thread that uses the custom operation to resize the cache during runtime.
# - Defines several insert threads are used to perform large transactions.
# - Defines custom tracking to save the transaction id and cache size as values. Keys are
# timestamps.
duration_seconds=20,
cache_size_mb=1,
workload_generator=
(
    custom_config=
    (
        op_rate=5s,
        thread_count=1
    ),
    insert_config=
    (
        key_size=10000,
        op_rate=1s,
        ops_per_transaction=(min=2,max=2),
        thread_count=1,
        value_size=1000000
    ),
    populate_config=
    (
        # Only one empty collection is required.
        collection_count=1
    ),
),
workload_tracking=
(
    tracking_key_format=Q,
    tracking_value_format=QQ
)