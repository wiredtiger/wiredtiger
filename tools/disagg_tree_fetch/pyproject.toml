[project]
name = "disagg-tree-fetch"
version = "0.1.0"
description = "Fetch, decrypt, decode, and traverse a WiredTiger disaggregated storage page tree"
requires-python = ">=3.10"
dependencies = [
    "grpcio>=1.60.0",
    "grpcio-tools>=1.60.0",
    "protobuf>=4.0.0",
]
